/// Centralized app-facing strings.
///
/// Keep this thin for now so we can swap to Flutter localization later
/// without chasing hardcoded titles across the widget tree.
abstract final class AppStrings {
  static const appDisplayName = 'BoomPrint';
}
